{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "reader-window",
  "description": "Restricted reader window capability for MyPersonalShelf",
  "windows": ["reader-*"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen"
  ]
}
